version: 0.1.0
icon: ./icon.png
scripts:
  bootstrap: |
    sudo apt-get update
    sudo apt-get install -y python3-opencv
    pip install --upgrade pip
    pip install -r requirements.txt
dependencies:
  oomol-preview: 0.0.3
name: pdf-craft-cloud
repository: https://github.com/oomol-flows/pdf-craft
releaseNotes: support LaTeX & table recognization.
displayName: PDF Craft Cloud
description: |
  PDF craft is a project that converts scanned PDF books into Markdown or EPUB formats using local AI
releaseType: package
